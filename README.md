# RTA_KL
git clone https://github.com/<login>/RTA_<inicjaly>.git
cd RTA_<inicjaly>
echo "# Analiza danych w czasie rzeczywistym" > README.md
git add . && git commit -m "Pierwszy commit" && git push origin main
